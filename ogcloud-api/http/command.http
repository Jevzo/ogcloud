@commandTarget = lobby

### Execute a command
POST {{baseUrl}}/api/v1/command
Authorization: Bearer {{accessToken}}
Content-Type: application/json

{
  "target": "{{commandTarget}}",
  "targetType": "group",
  "command": "say Scheduled restart in 5 minutes."
}
